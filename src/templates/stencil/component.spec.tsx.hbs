import { {{pascalCase componentName}} } from './{{dashCase componentName}}';

describe('{{pascalCase componentName}}', () => {
    it('builds', () => {
        expect(new {{pascalCase componentName}}()).toBeTruthy();
    });
});
